# Placeholder defaults so the app boots without external credentials.
# Real values are delivered via /run/base44/app.env (platform-managed) and override these.
APPS_SCRIPT_URL=
APPS_SCRIPT_TOKEN=
GEMINI_API_KEY=
LOVABLE_API_KEY=
